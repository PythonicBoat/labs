#include <stdio.h>
#include <stdlib.h>

struct node {
    int data;
    struct node *next;
};

void add(struct node *head, int k) {
    struct node *new = head;
    while(new!=NULL) {
        new->head += k;
    }
}

void delete(struct node *new, int pos) {
    struct node *new = head;
    free();
}

do{
    int choice;
    printf("Enter choice : ");
    scanf("%d", &choice);
    switch(choice);
    case 1:
        break;
    case 2:
        break;
} while(1);